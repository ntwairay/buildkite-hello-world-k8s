# Default values for helloworld-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

deployment:
    replicas: 3
  
image:
    repository: rayhub/hello-world
  
redis:
    enabled: false
